{
  "name": "perf-sync",
  "private": true,
  "description": "Performance harness for comparing the Nairobi sync protocol vs the Postgres-backed /api/v1/pg-sync endpoint.",
  "dependencies": {
    "pouchdb": "^7.3.1",
    "pouchdb-adapter-memory": "^7.3.1"
  },
  "scripts": {
    "test": "mocha 'test/**/*.spec.js'"
  }
}
